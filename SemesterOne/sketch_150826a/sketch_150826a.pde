// Tanner Jones
// Collision Detection

// Setup Runs Once
// Needs to be First
void setup() {
  size(200,200);
  background(255,0,255);
  
}

// Draw is a Loop
void draw() {
  line(mouseY,mouseX,width/2,height/2);
  
}
